function useOlevelRows(form){let index=1;const add=()=>{const first=form.querySelector('[data-olevel-rows] .admin-grid');const clone=first.cloneNode(true);clone.querySelectorAll('select').forEach((select)=>{select.name=select.name.replace(/results\[\d+\]/,`results[${index}]`);select.selectedIndex=0;});form.querySelector('[data-olevel-rows]').appendChild(clone);index+=1;};return{add};}const form=document.querySelector('#applicant-olevel-form');if(form){const editor=useOlevelRows(form);form.querySelector('[data-add-olevel-row]')?.addEventListener('click',editor.add);}
