import { ref } from 'vue';
/** Composition API autosave helper for the Phase 2 biodata draft island. */
export function useApplicantDraft(form) { const message=ref(''); let timer; const save=()=>{clearTimeout(timer); timer=setTimeout(async()=>{message.value='Saving draft…'; try{const body=Object.fromEntries(new FormData(form).entries()); const response=await fetch(form.dataset.autosaveEndpoint,{method:'PUT',headers:{'Content-Type':'application/json','X-Requested-With':'XMLHttpRequest',[form.dataset.csrfHeader]:form.dataset.csrfToken},body:JSON.stringify(body)}); const payload=await response.json(); message.value=response.ok?`${payload.message} (${payload.data.completion_percent}% complete)`:payload.message;}catch{message.value='Draft could not be saved. Retry shortly.';} form.querySelector('[data-autosave-message]').textContent=message.value;},700);}; return{message,save}; }
const form=document.querySelector('#applicant-biodata-form'); if(form){const draft=useApplicantDraft(form); form.addEventListener('input',draft.save);}
